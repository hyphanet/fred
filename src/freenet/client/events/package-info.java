/**
 * Client events layer. This layer provides a series of notifications (@see
 * ClientEvent ) of the progress of a high-level request to higher layers 
 * (e.g. FCP, fproxy) so they can e.g. render progress bars.
 */
package freenet.client.events;
